<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- ImageView para mostrar el logo de la página -->
    <ImageView
        android:id="@+id/ivLogo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:contentDescription="Logo de la página" />

    <!-- TextView para el encabezado -->
    <TextView
        android:id="@+id/tvHeader"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Últimas Noticias"
        android:textSize="20sp"
        android:textStyle="bold"
        android:paddingTop="16dp"
        android:gravity="center" />

    <!-- Contenedor para mostrar las 3 noticias -->
    <LinearLayout
        android:id="@+id/newsContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingTop="16dp">

        <!-- Aquí se irán añadiendo las noticias dinámicamente desde el código -->
    </LinearLayout>
</LinearLayout>
